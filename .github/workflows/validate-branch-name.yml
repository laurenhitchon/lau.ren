# Vendored from digitalnsw/nswds-devops for this personal repository.
# The reusable workflow is SHA-pinned below and updated through Renovate.
# The implementation lives in that repo's reusable-validate-branch-name.yml.
name: Validate Branch Name

on:
  pull_request:
    types: [opened, edited, reopened]

permissions:
  contents: read

jobs:
  check-branch-name:
    uses: digitalnsw/nswds-devops/.github/workflows/reusable-validate-branch-name.yml@a244014e317c6ab661d7c53ecd9152773b87c3be # v1
