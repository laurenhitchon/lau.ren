# Vendored from digitalnsw/nswds-devops for this personal repository.
# The reusable workflow is SHA-pinned below and updated through Renovate.
# The implementation lives in that repo's reusable-commitlint.yml.
name: Commitlint

on:
  pull_request:

permissions:
  contents: read

jobs:
  commitlint:
    uses: digitalnsw/nswds-devops/.github/workflows/reusable-commitlint.yml@6754b3494a47894d89e9d1b0106a64e13a1a9443 # v1
